/**
 * Constantes de LGPD sem dependência de servidor.
 *
 * Ficam separadas de lgpd-actions.ts porque um arquivo "use server" só pode
 * exportar funções assíncronas.
 */

/** Texto que o operador precisa digitar para confirmar uma exclusão definitiva. */
export const DELETE_CONFIRMATION = "EXCLUIR DEFINITIVAMENTE";
